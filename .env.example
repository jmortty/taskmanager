# Environment variables for Task Manager API

# Server Configuration
NODE_ENV=development # or production
PORT=5001

# MongoDB Connection
# Replace with your actual MongoDB connection string
# Example (local): MONGO_URI=mongodb://localhost:27017/taskmanager
# Example (Atlas): MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/taskmanager?retryWrites=true&w=majority
MONGO_URI=mongodb://localhost:27017/taskmanager

# JSON Web Token (JWT) Configuration
# Use a strong, random string for the secret in production
JWT_SECRET=your_very_secret_jwt_string_replace_me
JWT_EXPIRES_IN=30d # Example: token expires in 30 days

# Email Configuration (for Nodemailer - add later)
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USER=user@example.com
# EMAIL_PASS=your_email_password
# EMAIL_FROM=Task Manager <noreply@example.com>
